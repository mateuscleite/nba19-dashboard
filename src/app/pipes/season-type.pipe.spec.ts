import { SeasonTypePipe } from './season-type.pipe';

describe('SeasonTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SeasonTypePipe();
    expect(pipe).toBeTruthy();
  });
});
