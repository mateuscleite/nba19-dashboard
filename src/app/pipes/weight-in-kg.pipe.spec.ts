import { WeightInKgPipe } from './weight-in-kg.pipe';

describe('WeightInKgPipe', () => {
  it('create an instance', () => {
    const pipe = new WeightInKgPipe();
    expect(pipe).toBeTruthy();
  });
});
