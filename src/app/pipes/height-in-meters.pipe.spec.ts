import { HeightInMetersPipe } from './height-in-meters.pipe';

describe('HeightInMetersPipe', () => {
  it('create an instance', () => {
    const pipe = new HeightInMetersPipe();
    expect(pipe).toBeTruthy();
  });
});
